import 'package:flutter/material.dart';

/// class to define used [colors] in app
class AppColors {
  AppColors._();

  /// color for welcome [Handy] text
  static const Color handyTextColor = Color(0xFF3F3D56);

  /// primary color of app
  static const Color primaryColor = Color(0xFFC2D8FF);

  /// background color of pages
  static const Color appBackgroundColor = Color.fromRGBO(252, 252, 252, 1);
}
